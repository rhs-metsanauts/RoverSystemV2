#uses unix sockets to control all parts of the rover from a single interface
#uses a config file to determine which hardware components are present and how to control them
from Config import get_config

